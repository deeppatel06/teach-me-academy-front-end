//

// routes
// import { PATH_PAGE } from '../../routes/paths';

// ----------------------------------------------------------------------

const menuConfig = [
  { title: "Home", path: "/" },
  // { title: 'Order Report', path: PATH_PAGE.orderReport },
  // { title: 'Vehical Inspection Form', path: PATH_PAGE.inspectionForm },
];

export default menuConfig;
